{
  "name": "yt-transcript",
  "version": "1.0.0",
  "description": "YouTube Transcript Extractor Chrome Extension",
  "scripts": {
    "build": "powershell -Command \"if (Test-Path dist) { Remove-Item -Path dist\\* -Recurse -Force }; if (-not (Test-Path dist)) { New-Item -ItemType Directory -Path dist }; Copy-Item manifest.json,popup.html,popup.js,content.js -Destination dist\"",
    "clean": "powershell -Command \"if (Test-Path dist) { Remove-Item -Path dist\\* -Recurse -Force }\""
  },
  "keywords": ["chrome-extension", "youtube", "transcript", "extractor"],
  "author": "Tyler Hughes",
  "license": "MIT"
}
